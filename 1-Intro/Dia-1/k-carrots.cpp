#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
    int n, p;
    cin >> n >> p;
    string s;
    getline(cin, s);
    while(n--){
        getline(cin, s);
    }
    cout << p << endl;
}